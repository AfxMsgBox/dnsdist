# TODO

## 发布前验证

- [ ] 在全新 Debian/Ubuntu 主机上执行单文件安装，确认缺失 dnsdist 或其他依赖时只显示手动安装提示，不修改系统软件包。
- [ ] 使用实际 GitHub 压缩包执行一次正常更新和一次人为制造失败的回滚验证。
- [ ] 分别验证默认卸载和 `--purge` 彻底卸载。

后续可选改进项参见 `ARCH.md` 的“可演进方向”。新增未完成任务时，应在此记录范围、原因和验收条件。
